services:
  demopage:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - npm-network
    # ports:
    #   - "8080:80"

networks:
  npm-network:
    external: true